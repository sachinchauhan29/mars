<%- include("../shared/header"); %>


    <div class="app-page-title">
        <div class="page-title-wrapper">
            <div class="page-title-heading">
                <!-- <div class="page-title-icon">
                        <a href="./Upload-payout.html">
                            <i class=" metismenu-icon fa fa-cloud-upload  icon-gradient bg-mean-fruit">
                            </i>
                        </a>
                    </div> -->
                <div>
                    <a href="./upload-payout">
                        <i class="metismenu-icon fas fa-arrow-alt-circle-left"></i>
                    </a>
                    <b>UPLOAD HISTORY</b>
                </div>
            </div>

        </div>
    </div>

    <div class="row">
        <div class="col-md-12 col-lg-12 mx-auto">
            <div class="mb-3 ">
                <div class="card">
                    <div class="card-body">
                        <table id="example" class="table table-striped table-bordered table-responsive" cellspacing="0">
                            <thead>
                                <tr>
                                    <th class="tabel-width">UPLOADED DATE AND TIME</th>
                                    <th class="tabel-width">TOTAL RECORDS</th>
                                    <th class="tabel-width">VALID RECORDS</th>
                                    <th class="tabel-width">INVALID RECORDS</th>
                                    <th class="tabel-width">PROCESSING RECORDS</th>
                                    <th class="tabel-width">ACTION</th>


                                </tr>
                            </thead>

                            <% uploadDataArray.forEach((data)=> { %>
                                <tbody>
                                    <tr>
                                        <td>
                                            <% const originalDate=new Date(data.insert_datetime); %>
                                                <%= originalDate.toLocaleString('en-US', { timeZone: 'Asia/Kolkata' ,
                                                    year: 'numeric' , month: '2-digit' , day: '2-digit' ,
                                                    hour: '2-digit' , minute: '2-digit' , second: '2-digit' , hour12:
                                                    true }).replace(',', '' ) %>
                                        </td>
                                        <td>
                                            <%= data.total_count %>
                                        </td>
                                        <td>
                                            <%= data.success_count %>
                                        </td>
                                        <td>
                                            <%= data.failure_count %>
                                        </td>
                                        <td>
                                            <%= data.success_count %>
                                        </td>
                                        <td>
                                            <a class="Download" href="<%= data.updated_file_name %>"
                                                download>Download</a>
                                        </td>
                                    </tr>
                                </tbody>
                                <% }); %>


                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>


    <script>
        document.addEventListener('DOMContentLoaded', function () {
            var table = document.querySelector('#example');
            var dataTable = new DataTable(table, {

            });
        });
    </script>
    <%- include("../shared/footer"); %>